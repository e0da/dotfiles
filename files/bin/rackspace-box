#!/bin/sh

set -e # exit on first error

. $HOME/.rackspace-box

[ -z "$1" ] && echo "Supply a node name" && exit 1

knife rackspace server create \
  --image "$RSB_IMAGE" \
  --flavor "$RSB_FLAVOR" \
  --environment "$RSB_ENVIRONMENT" \
  --run-list "$RSB_RUN_LIST" \
  --node-name "$1"
